import React, { useState, useEffect, useRef } from 'react';
import axios from 'axios';

const ViewMedication = (props) => {
    return (
        <h2> Hello World! Medication ready for pickup</h2>
    )
};

export default ViewMedication;